<html>
<% if(current_user.role?("Developer") ) %>
	<%= render "developer", projects: @projects, todos: @todos %>
<% end %>

<% if(current_user.role?("Manager") ) %>
	<%= render 'manager' , projects: @projects , todos: @todos %>
<% end %>

</html>